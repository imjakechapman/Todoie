/**
** Module Dependencies
**/
// var todoController = require('../controllers/todoController');

module.exports = function(app, express, router) {

//   // Set route param (basically prefetching and doing anything with resource before route hits)
//   router.param('todo_id', todoController.load);
//   app.param('todo_id', todoController.load);

//   // Chaining Routes using app.route
//   app.route('/todos')
//     .get(todoController.list)
//     .post(todoController.create);


//   // Show and Do Edit
//   app.route('/todos/:todo_id/edit')
//     .get(todoController.edit)
//     .post(todoController.update);


//   // Complete Todo
//   router.get('/todos/:todo_id/complete', todoController.completeTodo);

//   // Delete Todo
//   router.get('/todos/:todo_id/delete', todoController.destroy);

//   // Remove Completed
//   router.get('/todos/removeCompleted', todoController.removeCompleted);

};
